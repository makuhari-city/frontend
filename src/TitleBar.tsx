const TitleBar = () => (
  <header>
    <nav className="flex items-center justify-between p-6 container mx-auto mt-32 mb-32 max-w-screen-lg">
      <a href="/" className="hover:underline">
        meta city
      </a>
    </nav>
  </header>
);

export default TitleBar;
